#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "fonctions.h"

int main()
{
    // printf("%s\n", inverseChaine("bonjour"));
    // printf("%s\n", litChaine());
    // printf("%s\n", litFichier("test.txt"));
    // printf("%s\n", litLigne("test.txt"));

    // char **arr = malloc(4 * sizeof(char *));
    // arr[0] = malloc(sizeof(char) * 4);
    // arr[0] = "zezr";
    // arr[1] = malloc(sizeof(char) * 5);
    // arr[1] = "er";
    // arr[2] = malloc(sizeof(char) * 6);
    // arr[2] = "aber";

    // char **res = trierChaines(arr);
    // for (int i = 0; i < 3; i++) {
    //     printf("%s\n", res[i]);
    // }

    // char **copie_arr = copierChaines(arr);
    // printf("%s\n", relierChaines(copie_arr));
    // printf("%s\n", copierChaines(arr));

    // int **mat = saisirMatrice(3);
    // afficherMatrice(3, mat);
    // afficherMatriceFichier(3, mat, "mat.txt");
    // libererMatrice(3, mat);

    // int **m1 = saisirMatrice(2);
    // int **m2 = saisirMatrice(2);
    // afficherMatrice(2, multiplierMatrices(2, m1, m2));
    // libererMatrice(2, m1);
    // libererMatrice(2, m2);
    
    // Point *p1 = saisirPoint();
    // Point *p2 = saisirPoint();
    // Point *mid = milieuPoint(p1, p2);

    // printf("X: %f - Y: %f\n", mid->x, mid->y);

    // free(p1);
    // free(p2);
    // free(mid);

    return 0;
}